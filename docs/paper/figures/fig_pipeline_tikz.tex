% Orchestrator pipeline (compact vertical flow).
\begin{tikzpicture}[
  font=\tiny\sffamily,
  node distance=0.2cm,
  box/.style={draw, rounded corners=1.5pt, inner sep=1.5pt, align=center},
  arr/.style={-{Stealth[length=1mm]}, line width=0.3pt}
]
  \node[box, fill=gray!8] (q) {Question};
  \node[box, fill=blue!5, below=of q] (pl) {Planner};
  \node[box, fill=green!7, below=of pl] (sch) {search\_datasets};
  \node[box, fill=orange!10, below=of sch] (sel) {Selector};
  \node[box, fill=violet!12, below=of sel] (exec) {GA or TA};
  \node[box, fill=cyan!8, below=of exec] (syn) {Synthesis};
  \node[box, fill=gray!12, below=of syn] (out) {AgentResponse};

  \draw[arr] (q) -- (pl);
  \draw[arr] (pl) -- node[right, font=\tiny] {loop} (sch);
  \draw[arr] (sch) -- (sel);
  \draw[arr] (sel) -- node[right, font=\tiny] {per ds.} (exec);
  \draw[arr] (exec) -- (syn);
  \draw[arr] (syn) -- (out);
\end{tikzpicture}
