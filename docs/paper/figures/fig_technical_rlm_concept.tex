% Conceptual Technical path: context + iterative code execution -> evidence (framework-level).
\begin{tikzpicture}[
  font=\scriptsize\sffamily,
  box/.style={draw, rounded corners=2pt, inner sep=2pt, align=center},
  arr/.style={-{Stealth[length=1.2mm]}, line width=0.35pt},
  lbl/.style={font=\tiny\sffamily, inner sep=0pt}
]
  \node[box, fill=gray!10, text width=2.45cm] (ctx) {\textbf{Technical context}\\[1pt]
    schema summaries, row previews,\\optional unstructured text};
  \node[box, fill=orange!12, below=0.26cm of ctx, minimum width=2.2cm] (lm) {Code-capable LM\\(e.g.\ RLM-style)};
  \node[box, fill=cyan!12, right=0.32cm of lm] (sb) {Sandbox\\execution};
  \node[box, fill=green!10, below=0.38cm of lm, minimum width=2.85cm] (ev) {Structured evidence};

  \draw[arr] (ctx) -- (lm);
  \draw[arr] (lm) to[bend left=18] node[lbl, above, pos=0.55] {actions} (sb);
  \draw[arr] (sb) to[bend left=18] node[lbl, below, pos=0.45] {observations} (lm);
  \draw[arr] (lm) -- (ev);
\end{tikzpicture}
